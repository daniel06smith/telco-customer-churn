# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Virtual environment
.venv/
venv/
env/

# Data — raw data is never committed; processed can be regenerated
data/processed/

# Models — track the trained artefacts needed by the app
# (git can't un-ignore files inside an ignored directory, so we track the
# whole models/ folder and only exclude MLflow's own run storage)
/mlruns/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# MLflow SQLite database — regenerated each run
mlflow.db

# Claude Code local settings
.claude/
